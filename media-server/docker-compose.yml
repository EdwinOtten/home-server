services:

  init-folders:
    image: alpine
    container_name: media-server-init
    volumes:
      - media-server-storage-root:/mnt/storage
    # This command creates all subdirectories at once
    command: >
      sh -c "mkdir -p /mnt/storage/media 
      /mnt/storage/services/sonarr 
      /mnt/storage/services/radarr 
      /mnt/storage/services/configarr 
      /mnt/storage/services/prowlarr 
      /mnt/storage/services/bazarr 
      /mnt/storage/services/jellyfin 
      /mnt/storage/services/seerr 
      /mnt/storage/services/sabnzbd 
      /mnt/storage/services/qbittorrent"

  # qBitorrent is used to download torrents
  # qbittorrent:
  #   image: lscr.io/linuxserver/qbittorrent:4.6.3
  #   container_name: qbittorrent
  #   environment:
  #     - PUID=${PUID}
  #     - PGID=${PGID}
  #     - WEBUI_PORT=8081
  #   volumes:
  #     - /etc/localtime:/etc/localtime:ro
  #     - ${MEDIA_DIRECTORY}:/data
  #     - ${INSTALL_DIRECTORY}/config/qbittorrent:/config
  #   restart: unless-stopped
  #   ports: # qbittorrent
  #     - 8081:8081 # qbittorrent

  # SABnzbd is used to download from usenet
  # sabnzbd:
  #   image: lscr.io/linuxserver/sabnzbd:latest
  #   container_name: sabnzbd
  #   environment:
  #     - PUID=${PUID}
  #     - PGID=${PGID}
  #     - TZ=America/Montevideo
  #   volumes:
  #     - /etc/localtime:/etc/localtime:ro
  #     - ${MEDIA_DIRECTORY}:/data
  #     - ${INSTALL_DIRECTORY}/config/sabnzbd:/config
  #   ports: # sabnzbd
  #     - 8080:8080 # sabnzbd
  #   restart: unless-stopped

  # Sonarr is used to query, add downloads to the download queue and index TV shows
  # https://sonarr.tv/
  sonarr:
    image: lscr.io/linuxserver/sonarr
    container_name: sonarr2
    environment:
      - PUID=${PUID?:error}
      - PGID=${PGID?:error}
      - TZ=${TZ?:error}
      - SONARR__AUTH__ENABLED=TRUE
      - SONARR__AUTH__METHOD=Forms
      - SONARR__AUTH__APIKEY=${SONARR_API_KEY:?error}
      - SONARR__AUTH__REQUIRED=DisabledForLocalAddresses
    volumes:
      - media:/media
      - sonarr-storage:/config
    ports:
      - 18989:8989
    restart: unless-stopped
    networks:
      - media-server-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H 'X-Api-Key: ${SONARR_API_KEY:?error}' http://localhost:8989/api/v3/health || exit 1"]
      interval: 1m
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      init-folders:
        condition: service_completed_successfully

  # Radarr is used to query, add downloads to the download queue and index Movies
  # https://radarr.video/
  radarr:
    image: lscr.io/linuxserver/radarr
    container_name: radarr2
    environment:
      - PUID=${PUID?:error}
      - PGID=${PGID?:error}
      - TZ=${TZ?:error}
      - RADARR__AUTH__ENABLED=TRUE
      - RADARR__AUTH__METHOD=Forms
      - RADARR__AUTH__APIKEY=${RADARR_API_KEY:?error}
      - RADARR__AUTH__REQUIRED=DisabledForLocalAddresses
    volumes:
      - media:/media
      - radarr-storage:/config
    ports:
      - 17878:7878
    restart: unless-stopped
    networks:
      - media-server-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H 'X-Api-Key: ${RADARR_API_KEY:?error}' http://localhost:7878/api/v3/health || exit 1"]
      interval: 1m
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      init-folders:
        condition: service_completed_successfully

  # Configarr is used to manage quality profilea and custom formats. Configarr pushes theses to Radarr/Sonarr.
  #configarr:
  #  image: ghcr.io/raydak-labs/configarr:1.17.2
  #  container_name: configarr
  #  user: ${PUID?:error}:${PGID?:error}
  #  depends_on:
  #    - sonarr
  #  environment:
  #    - TZ=${TZ?:error}
  #  volumes:
  #    - /opt/configarr-cache:/app/repos # Cache repositories
  #    - /opt/home-server-data/github.com/EdwinOtten/home-server/media-server/configarr/config:/app/config # Contains the config.yml and secrets.yml
  #    - /opt/home-server-data/github.com/EdwinOtten/home-server/media-server/configarr/templates:/app/templates # Optional if custom templates
  #    # - /opt/home-server-data/github.com/EdwinOtten/home-server/media-server/configarr/custom-formats:/app/cfs # Optional if custom formats locally provided
  #  # restart: "no" # optional make sure this is set to no or removed. Default is no
  #  networks:
  #    - media-server-network


  # Prowlarr is our torrent indexer/searcher. Preolarr pushes indexes to Sonarr/Radarr.
  # https://prowlarr.com/
  prowlarr:
    image: lscr.io/linuxserver/prowlarr
    container_name: prowlarr2
    environment:
      - PUID=${PUID?:error}
      - PGID=${PGID?:error}
      - TZ=${TZ?:error}
      - PROWLARR__AUTH__ENABLED=TRUE
      - PROWLARR__AUTH__METHOD=Forms
      - PROWLARR__AUTH__APIKEY=${PROWLARR_API_KEY:?error}
      - PROWLARR__AUTH__REQUIRED=DisabledForLocalAddresses
    volumes:
      - prowlarr-storage:/config
    ports:
      - 19696:9696
    restart: unless-stopped
    networks:
      - media-server-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H 'X-Api-Key: ${PROWLARR_API_KEY:?error}' http://localhost:9696/api/v1/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      init-folders:
        condition: service_completed_successfully
      sonarr:
        condition: service_healthy
      radarr:
        condition: service_healthy

  # Bazarr is used to query, add downloads to the download queue and index subtitles
  # https://www.bazarr.media/
  bazarr:
    image: lscr.io/linuxserver/bazarr
    container_name: bazarr2
    environment:
      - PUID=${PUID?:error}
      - PGID=${PGID?:error}
      - TZ=${TZ?:error}
      - BAZARR_API_KEY=${BAZARR_API_KEY:?error}
      - SONARR_API_KEY=${SONARR_API_KEY:?error}
      - RADARR_API_KEY=${RADARR_API_KEY:?error}
      - SONARR_PORT=18989
      - RADARR_PORT=17878
    volumes:
      - media:/media
      - bazarr-storage:/config
      # Mount the init script into the LSIO init directory
      # see https://docs.linuxserver.io/general/container-customization/#custom-scripts
      - /opt/home-server-data/github.com/EdwinOtten/home-server/media-server/bazarr/init-config.sh:/custom-cont-init.d/init-config.sh:ro
    ports:
      - 16767:6767
    restart: unless-stopped
    networks:
      - media-server-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H 'X-Api-Key: ${BAZARR_API_KEY:?error}' http://localhost:6767/api/system/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      init-folders:
        condition: service_completed_successfully
      sonarr:
        condition: service_healthy
      radarr:
        condition: service_healthy

  # Jellyfin is a media server that allows client devices to stream content.
  # https://jellyfin.org/
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin2
    user: ${PUID}:${PGID}
    ports:
      - 18096:8096/tcp
      - 17359:7359/udp
    volumes:
      - jellyfin-storage:/config
# not mapping cache directory should create a non-persistent volume
#      - jellyfin-cache:/cache
      - media:/media
      - old-downloads:/old-downloads
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
    restart: 'unless-stopped'
    networks:
      - media-server-network
    depends_on:
      init-folders:
        condition: service_completed_successfully

  # Seerr allows users to submit content requests, which are forwarded to 
  # Radarr/Sonarr after an optional approval.
  # https://seerr.dev/
  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    init: true
    container_name: seerr
    environment:
      - LOG_LEVEL=debug
      - TZ=${TZ?:error}
      - PORT=5055 #optional
      - API_KEY=${SEERR_API_KEY:?error}
    ports:
      - 5055:5055
    volumes:
      - seerr-storage:/app/config
    restart: unless-stopped
    networks:
      - media-server-network
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:5055/api/v1/status || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
      retries: 3
    depends_on:
      init-folders:
        condition: service_completed_successfully

networks:
  media-server-network:
    driver: bridge

volumes:
  old-downloads:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nolock,ro,soft,nfsvers=4
      device: ":/volume1/downloads"

  media-server-storage-root:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage"

  media:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/media"

  sonarr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/sonarr"

  radarr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/radarr"

  configarr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/configarr"

  prowlarr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/prowlarr"

  bazarr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/bazarr"

  jellyfin-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/jellyfin"

  seerr-storage:
    driver: local
    driver_opts:
      type: nfs
      o: addr=192.168.1.111,nfsvers=4,rw
      device: ":/volume1/media-server-storage/services/seerr"
